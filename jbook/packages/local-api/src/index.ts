export const serve = (port: number, filename: string, dir: string) => {
    console.log('Serve traffic on port', port);
    console.log('fetching cells from', filename);
    console.log('file in dir', dir);
};
